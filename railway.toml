[nixpacks]
buildCommand = "cd backend && npm install && npx tsc"
startCommand = "cd backend && node dist/server.js"

[phases.build]
dependsOn = ["setup"]
cmds = ["cd backend && npm install && npx tsc"]

[phases.setup]
cmds = ["apt-get update && apt-get install -y nodejs npm"]
