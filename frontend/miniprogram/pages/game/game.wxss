/* ===== 全屏布局 - 横竖屏自适应 ===== */
page {
  width: 100%;
  height: 100vh;
  overflow: hidden;
  margin: 0;
  padding: 0;
}

.game-page {
  width: 100%;
  height: 100vh;
  background: radial-gradient(ellipse at center, #2d8e4e 0%, #1a6e3a 40%, #0f5c2b 100%);
  position: relative;
  overflow: hidden;
  display: flex;
  flex-direction: column;
}

/* 桌布纹理 */
.game-page::before {
  content: '';
  position: absolute;
  top: 0; left: 0; right: 0; bottom: 0;
  background:
    repeating-linear-gradient(0deg, transparent, transparent 50rpx, rgba(255,255,255,0.015) 50rpx, rgba(255,255,255,0.015) 51rpx),
    repeating-linear-gradient(90deg, transparent, transparent 50rpx, rgba(255,255,255,0.015) 50rpx, rgba(255,255,255,0.015) 51rpx);
  pointer-events: none;
  z-index: 0;
}

/* 金色边框装饰 */
.game-page::after {
  content: '';
  position: absolute;
  top: 20rpx; left: 20rpx; right: 20rpx; bottom: 20rpx;
  border: 4rpx solid rgba(255,215,0,0.2);
  border-radius: 40rpx;
  pointer-events: none;
  z-index: 0;
}

/* ===== 顶部状态栏 ===== */
.status-bar {
  position: relative;
  z-index: 20;
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16rpx 24rpx;
  background: rgba(0,0,0,0.6);
  flex-shrink: 0;
}

.back-btn, .help-btn, .piggy-icon {
  width: 64rpx;
  height: 64rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  color: white;
  font-size: 32rpx;
  border-radius: 12rpx;
  background: rgba(255,255,255,0.1);
}

/* ===== 等待大厅 ===== */
.lobby-stage {
  position: relative;
  z-index: 1;
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}

.table-title {
  font-size: 56rpx;
  font-weight: bold;
  color: #ffd700;
  text-shadow: 0 4rpx 12rpx rgba(0,0,0,0.5);
  letter-spacing: 8rpx;
  margin-bottom: 40rpx;
}

/* 桌子中央区域 - 四方位布局 */
.table-center {
  position: relative;
  width: 90vw;
  height: 60vh;
  max-height: 600rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}

/* 玩家座位 */
.player-seat {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
  transition: all 0.3s ease;
}

.seat-top {
  order: 1;
  margin-bottom: 20rpx;
}

.seat-left {
  order: 2;
  position: absolute;
  left: 0;
  top: 50%;
  transform: translateY(-50%);
}

.seat-right {
  order: 3;
  position: absolute;
  right: 0;
  top: 50%;
  transform: translateY(-50%);
}

.seat-bottom {
  order: 4;
  margin-top: 20rpx;
}

/* 头像 */
.seat-avatar {
  width: 80rpx;
  height: 80rpx;
  border-radius: 50%;
  background: rgba(0,0,0,0.4);
  border: 3rpx solid rgba(255,255,255,0.3);
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.3s ease;
}

.seat-avatar.ready {
  border-color: #22c55e;
  box-shadow: 0 0 20rpx rgba(34,197,94,0.5);
  background: rgba(34,197,94,0.2);
}

.avatar-img {
  width: 100%;
  height: 100%;
  border-radius: 50%;
}

.avatar-letter {
  font-size: 36rpx;
  font-weight: bold;
  color: white;
}

/* 座位名字 */
.seat-name {
  font-size: 24rpx;
  color: rgba(255,255,255,0.9);
  max-width: 120rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  text-align: center;
}

.self-name {
  color: #ffd700;
  font-weight: bold;
}

/* 座位状态 */
.seat-status {
  font-size: 20rpx;
  padding: 4rpx 16rpx;
  border-radius: 20rpx;
  background: rgba(0,0,0,0.3);
  color: rgba(255,255,255,0.6);
}

.seat-status.ready {
  background: rgba(34,197,94,0.3);
  color: #22c55e;
}

/* 座位牌数 */
.seat-cards {
  font-size: 20rpx;
  color: rgba(255,255,255,0.5);
}

/* 已准备座位高亮 */
.ready-seat .seat-avatar {
  border-color: #22c55e;
  box-shadow: 0 0 20rpx rgba(34,197,94,0.5);
}

/* 中间按钮区 */
.center-buttons {
  display: flex;
  flex-direction: column;
  gap: 24rpx;
  align-items: center;
  margin-top: 40rpx;
}

.action-btn {
  width: 240rpx;
  height: 80rpx;
  line-height: 80rpx;
  font-size: 30rpx;
  border-radius: 40rpx;
  border: none;
  padding: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8rpx;
  transition: all 0.3s;
}

.ready-btn {
  background: linear-gradient(135deg, #22c55e, #16a34a);
  color: white;
  box-shadow: 0 4rpx 16rpx rgba(34,197,94,0.4);
}

.ready-btn.active {
  background: linear-gradient(135deg, #16a34a, #15803d);
  box-shadow: 0 4rpx 20rpx rgba(34,197,94,0.6);
}

.start-btn {
  background: linear-gradient(135deg, #f59e0b, #d97706);
  color: white;
  box-shadow: 0 4rpx 16rpx rgba(245,158,11,0.4);
}

.start-btn.disabled {
  opacity: 0.4;
  pointer-events: none;
}

.btn-icon {
  font-size: 32rpx;
  font-weight: bold;
}

/* ===== 游戏进行阶段 ===== */
.game-stage {
  position: relative;
  z-index: 1;
  flex: 1;
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

/* 左上角得分面板 */
.score-panel {
  position: absolute;
  top: 20rpx;
  left: 20rpx;
  z-index: 15;
  background: rgba(0,0,0,0.6);
  border-radius: 16rpx;
  padding: 20rpx 24rpx;
  border: 2rpx solid rgba(255,215,0,0.3);
  min-width: 240rpx;
}

.score-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 8rpx 0;
}

.score-label {
  font-size: 24rpx;
  color: rgba(255,255,255,0.7);
}

.score-value {
  font-size: 32rpx;
  font-weight: bold;
  color: #ffd700;
}

.team-a {
  color: #4ade80;
}

.team-b {
  color: #f87171;
}

.score-divider {
  height: 1rpx;
  background: rgba(255,255,255,0.1);
  margin: 12rpx 0;
}

.bid-info {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.bid-label {
  font-size: 24rpx;
  color: rgba(255,255,255,0.6);
}

.bid-value {
  font-size: 28rpx;
  font-weight: bold;
  color: #ffd700;
}

/* 对手区域 */
.opponents-area {
  position: relative;
  width: 100%;
  height: 100%;
  flex: 1;
  min-height: 0;
}

.opponent {
  position: absolute;
  display: flex;
  flex-direction: column;
  align-items: center;
  z-index: 5;
  transition: all 0.3s ease;
}

.opponent.active .player-card {
  border-color: #ffd700;
  box-shadow: 0 0 20rpx rgba(255,215,0,0.5);
}

.opponent-top {
  top: 10rpx;
  left: 50%;
  transform: translateX(-50%);
}

.opponent-left {
  top: 50%;
  left: 10rpx;
  transform: translateY(-50%);
}

.opponent-right {
  top: 50%;
  right: 10rpx;
  transform: translateY(-50%);
}

.player-card {
  background: rgba(0,0,0,0.4);
  border-radius: 16rpx;
  padding: 12rpx 16rpx;
  border: 2rpx solid rgba(255,255,255,0.2);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
  min-width: 120rpx;
}

.mini-avatar {
  width: 48rpx;
  height: 48rpx;
  border-radius: 50%;
  background: rgba(255,255,255,0.2);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 24rpx;
  font-weight: bold;
  color: white;
}

.mini-avatar.self {
  background: linear-gradient(135deg, #f59e0b, #d97706);
}

.player-name {
  font-size: 22rpx;
  color: rgba(255,255,255,0.9);
  max-width: 100rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  text-align: center;
}

.card-count {
  font-size: 20rpx;
  color: rgba(255,255,255,0.5);
}

.hand-back-stack {
  display: flex;
  gap: -8rpx;
}

.back-card {
  width: 24rpx;
  height: 36rpx;
  background: linear-gradient(135deg, #1e40af, #3b82f6);
  border-radius: 4rpx;
  border: 1rpx solid rgba(255,255,255,0.3);
  margin-left: -8rpx;
}

/* 叫分/出牌区 */
.trick-area {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 60%;
  height: 40%;
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 10;
}

.trick-center {
  position: relative;
  width: 100%;
  height: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 24rpx;
}

.bid-status {
  font-size: 32rpx;
  color: rgba(255,255,255,0.8);
  text-shadow: 0 2rpx 8rpx rgba(0,0,0,0.5);
}

.suit-options {
  display: flex;
  gap: 16rpx;
  flex-wrap: wrap;
  justify-content: center;
}

.suit-option {
  width: 80rpx;
  height: 80rpx;
  border-radius: 16rpx;
  background: white;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 36rpx;
  color: #333;
  border: 3rpx solid #ddd;
  box-shadow: 0 4rpx 12rpx rgba(0,0,0,0.3);
  transition: all 0.2s;
}

.suit-option.nt {
  background: rgba(255,255,255,0.3);
  color: rgba(255,255,255,0.7);
  font-size: 24rpx;
  font-weight: bold;
}

.suit-option.spade {
  color: #1e293b;
}

.suit-option.heart {
  color: #dc2626;
}

.suit-option.club {
  color: #1e293b;
}

.suit-option.diamond {
  color: #dc2626;
}

/* 出牌阶段：已出的牌 */
.played-cards {
  position: relative;
  width: 100%;
  height: 100%;
}

.played-card {
  position: absolute;
  width: 80rpx;
  height: 110rpx;
  background: white;
  border-radius: 8rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 4rpx 16rpx rgba(0,0,0,0.4);
  opacity: 0;
  transform: scale(0.5);
  transition: all 0.3s ease-out;
}

.played-card.show {
  opacity: 1;
  transform: scale(1);
}

.card-text {
  font-size: 26rpx;
  font-weight: bold;
  color: #333;
}

.played-card.top {
  top: -30rpx;
  left: 50%;
  transform: translateX(-50%) scale(0.5);
}

.played-card.top.show {
  transform: translateX(-50%) scale(1);
}

.played-card.bottom {
  bottom: -30rpx;
  left: 50%;
  transform: translateX(-50%) scale(0.5);
}

.played-card.bottom.show {
  transform: translateX(-50%) scale(1);
}

.played-card.left {
  top: 50%;
  left: -40rpx;
  transform: translateY(-50%) scale(0.5);
}

.played-card.left.show {
  transform: translateY(-50%) scale(1);
}

.played-card.right {
  top: 50%;
  right: -40rpx;
  transform: translateY(-50%) scale(0.5);
}

.played-card.right.show {
  transform: translateY(-50%) scale(1);
}

.placeholder {
  color: rgba(255,255,255,0.3);
  font-size: 28rpx;
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
}

/* ===== 下半部分：操作栏 + 手牌 + 底部信息 ===== */
.game-bottom {
  position: relative;
  z-index: 10;
  display: flex;
  flex-direction: column;
  flex-shrink: 0;
}

/* 操作按钮栏 */
.action-bar {
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: center;
  gap: 16rpx;
  flex-wrap: wrap;
  padding: 12rpx 20rpx;
  background: rgba(0,0,0,0.3);
}

.bar-btn {
  height: 56rpx;
  padding: 0 24rpx;
  border-radius: 12rpx;
  font-size: 26rpx;
  border: none;
  color: white;
  display: flex;
  align-items: center;
  justify-content: center;
}

.pass-btn {
  background: #666;
}

.bid-btn {
  background: #2563eb;
}

.play-btn.enabled {
  background: #22c55e;
}

.play-btn.disabled {
  background: #666;
  opacity: 0.5;
}

.cancel-btn {
  background: #ef4444;
}

.waiting-text {
  color: rgba(255,255,255,0.6);
  font-size: 26rpx;
}

/* 我的手牌 (底部横排) */
.my-hand {
  padding: 16rpx 20rpx 8rpx;
  background: linear-gradient(to top, rgba(0,0,0,0.5), rgba(0,0,0,0.2));
}

.hand-scroll {
  width: 100%;
}

.hand-cards {
  display: flex;
  gap: 0;
  padding-bottom: 8rpx;
}

.card {
  width: 68rpx;
  height: 96rpx;
  background: white;
  border-radius: 6rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-right: -20rpx;
  transition: all 0.2s;
  border: 2rpx solid #ddd;
  flex-shrink: 0;
  cursor: pointer;
}

.card:first-child {
  margin-left: 10rpx;
}

.card:last-child {
  margin-right: 10rpx;
}

.card.selected {
  margin-top: -20rpx;
  border-color: #ffd700;
  box-shadow: 0 -8rpx 20rpx rgba(255,215,0,0.5);
}

.card.playable {
  opacity: 1;
}

.card.unplayable {
  opacity: 0.4;
}

/* 底部玩家信息栏 */
.bottom-bar {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 16rpx;
  padding: 8rpx 20rpx 16rpx;
  background: rgba(0,0,0,0.4);
}

.my-info {
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.my-info .mini-avatar.self {
  background: linear-gradient(135deg, #f59e0b, #d97706);
}

.my-name {
  font-size: 26rpx;
  color: white;
  font-weight: bold;
}

.info-item {
  font-size: 20rpx;
  color: rgba(255,255,255,0.6);
}
